
function Posts() {
  return (
    <>
      posts component
      {/* {
        posts.map((url, i) => (
          <img key={`image-${i}`} src={url} alt="..." />
        ))
      } */}
    </>
  )
}

export default Posts;
